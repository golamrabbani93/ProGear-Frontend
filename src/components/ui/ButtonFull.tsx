import {Button} from 'antd';

const ButtonFull = () => {
	return (
		<div>
			<Button className="bg-primary border-0 hover:!bg-secondary !text-white font-bold w-full">
				VIEW CART
			</Button>
		</div>
	);
};

export default ButtonFull;
